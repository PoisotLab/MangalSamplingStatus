name: "Create website"

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1.0.0
      - name: "ls"
        run: ls -lah
      - name: "Move"
        run: cd dist
      - name: "ls"
        run: ls -lah
